namespace JumpingJack
{
    /// <summary>
    /// Player animation triggers
    /// </summary>
    public enum Triggers
    {
        Jump,
        Stand,
        BadJump,
        RunLeft,
        RunRight,
        Stun,
        Fall
    }

    /// <summary>
    /// Player animator floats
    /// </summary>
    public enum Floats
    {
        JumpSpeed,
        FallSpeed
    }
}
